--index-url https://test.pypi.org/simple/
--extra-index-url https://pypi.org/simple/

fastapi>=0.100
uvicorn[standard]>=0.30
pydantic>=2.0

# Database
sqlalchemy>=2.0
databases[postgresql,sqlite]>=0.8
asyncpg>=0.29  # postgres driver
aiosqlite>=0.19  # sqlite async driver

# Authentication
argon2-cffi>=23.0
python-multipart>=0.0.6

# Enlace adapter (published to test.pypi, fallback to pypi for dependencies)
enlace-fastapi>=0.0.1.dev34459610476
