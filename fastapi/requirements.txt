--index-url https://test.pypi.org/simple/
--extra-index-url https://pypi.org/simple/

# Core dependencies (pinned to stable versions)
fastapi>=0.100.0,<1.0.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0,<3.0.0

# Database
greenlet>=3.0.0  # Required by SQLAlchemy for async operations
sqlalchemy>=2.0.0,<3.0.0
databases[postgresql,sqlite]>=0.8.0
asyncpg>=0.29.0
aiosqlite>=0.19.0

# Authentication
argon2-cffi>=23.0.0
python-multipart>=0.0.6

# Enlace adapter (latest dev build from test.pypi)
enlace-fastapi>=0.0.1.dev34459610476
